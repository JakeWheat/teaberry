check:
  1 + 2 is 3
  3 - 2 is 1
  5 * 3 is 15
  1 / 2 is 0.5
  1764 * 1729 * 1764 * 1729 * 1764 * 1729 * 1764 * 1729
    is 86531512776056800758948096
  0 - 1 is -1
  # this is a parsing bug:
  # -1 is 0 - 1
  # I think the issue is that the parser behaves as is unary - has
  # lower precendence than 'is'
  # so it tries to parse this as -(1 is (0 - 1))
  # which fails with 'is' operator not recognised
end
