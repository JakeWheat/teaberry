
fun square(n):
  n * n
end

fun apply-twice(f, x):
  f(f(x))
where:
  apply-twice(square, 2) is 16
  apply-twice(square, 3) is 81
end

fun g():
  fun h():
    5
  where:
    h() is 5
  end
  # todo: the code still runs without true here, and it shouldn't
  true
end

# runs the nested test twice:
# todo: pyret makes these two blocks different instead of
# combining them into one, two is better

g()
g()
